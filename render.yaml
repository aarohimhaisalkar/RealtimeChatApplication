services:
  - type: web
    name: chat-app
    env: python
    plan: free
    buildCommand: echo "No build needed"
    startCommand: python test_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
    healthCheckPath: /
    autoDeploy: true
